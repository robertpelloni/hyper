sed -i 's/```bash\nexport CSC_IDENTITY_AUTO_DISCOVERY=false\nyarn run dist\n```/```bash\nexport CSC_IDENTITY_AUTO_DISCOVERY=false\nyarn run dist\n```\nIf code signing still fails due to missing keychain identities on a developer machine, ensure your Apple Developer certificate is present in your login keychain or use an ad-hoc signature via `codesign --force --deep --sign - dist\/mac\/TormentNexus.app`./g' DEPLOY.md
